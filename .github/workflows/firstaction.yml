name: First workflow
on: workflow_dispatch
jobs:
  fist-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "Hello world"
      - name: print goodbye
        run: echo "Done - bye!"
